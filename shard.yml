name: text-compressor
version: 1.0.0

authors:
  - Andrew Wei <awei82>

description: |
  A small library of algorithms for text compression

targets:
  text-compressor:
    main: src/cli.cr
  benchmark:
    main: src/benchmark.cr

crystal: '>= 1.14.0'

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
